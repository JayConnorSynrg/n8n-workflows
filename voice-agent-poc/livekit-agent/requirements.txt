# LiveKit Agents Voice Bot - Requirements
# Open-source alternative to OpenAI Realtime API

# Core LiveKit Agents Framework
livekit-agents>=0.8.0
livekit>=0.12.0

# STT (Speech-to-Text) Options
livekit-plugins-deepgram>=0.6.0  # Cloud STT (fast, accurate)
# livekit-plugins-silero>=0.6.0  # Local VAD
openai-whisper>=20231117        # Local STT (slower but free)

# LLM Options - choose based on your needs
openai>=1.0.0                   # OpenAI GPT-4 (fastest)
# ollama>=0.1.0                 # Local LLM via Ollama
# anthropic>=0.18.0             # Claude API

# TTS (Text-to-Speech) Options
livekit-plugins-openai>=0.6.0   # OpenAI TTS (great quality)
livekit-plugins-elevenlabs>=0.6.0  # ElevenLabs (best quality)
# livekit-plugins-cartesia>=0.6.0   # Cartesia Sonic (streaming)
# pyttsx3>=2.90                 # Local TTS (free but lower quality)

# Audio Processing
numpy>=1.24.0
librosa>=0.10.0                 # Time-stretching for speed control
soundfile>=0.12.0

# WebSocket & HTTP
websockets>=12.0
aiohttp>=3.9.0
httpx>=0.26.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.23.0
