version: '3.8'

services:
  # WebSocket Relay Server
  relay-server:
    build: ./relay-server
    container_name: voice-agent-relay
    ports:
      - "3000:3000"   # WebSocket
      - "3001:3001"   # Health check
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - PORT=3000
      - HEALTH_PORT=3001
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Voice Agent Webpage (Nginx)
  client:
    image: nginx:alpine
    container_name: voice-agent-client
    ports:
      - "8080:80"
    volumes:
      - ./client:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - relay-server
    restart: unless-stopped

# Optional: Add Traefik/Nginx reverse proxy for SSL
# networks:
#   default:
#     external:
#       name: traefik-network
