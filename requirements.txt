# Core LiveKit — ALL pinned to exact 1.3.12 (conn_options fix)
livekit==1.0.23
livekit-api>=1.0.0,<2.0.0
livekit-agents[mcp]==1.3.12
livekit-plugins-silero==1.3.12
livekit-plugins-deepgram==1.3.12
livekit-plugins-cartesia==1.3.12
livekit-plugins-turn-detector==1.3.12

# LLM (Cerebras via OpenAI-compatible API)
livekit-plugins-openai==1.3.12

# HTTP
aiohttp>=3.9.0
httpx>=0.27.0

# Configuration
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Observability
structlog>=24.0.0
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0

# Security
cryptography>=42.0.0

# MCP Integration
# livekit-agents[mcp] above includes native MCP client support
composio>=0.7.0
